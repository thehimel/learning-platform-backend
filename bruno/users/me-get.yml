info:
  name: me-get
  type: http
  seq: 2

http:
  method: GET
  url: "{{users}}/me"
  auth:
    type: bearer
    token: "{{admin_access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

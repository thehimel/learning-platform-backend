info:
  name: student-login
  type: http
  seq: 6

http:
  method: POST
  url: "{{auth}}/jwt/login"
  body:
    type: form-urlencoded
    data:
      - name: username
        value: "{{student_username}}"
      - name: password
        value: "{{student_password}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
